<%= link_to "Add Item",new_item_path  %>
<h1>Items</h1>
<% @items.each do |item| %>
  <h2><%= item.name %></h2>
  <h4><%= item.qty %></h4>
  <h4><%= item.price %></h4>
  <%= link_to "Edit", edit_item_path(item), class: 'btn'%>
<% end %>
