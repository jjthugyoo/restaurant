<h1>Dishes</h1>
<% @dishes.each do |dish| %>
  <h3> <%= dish.name %></h3>
  <h4><%= dish.price %></h4>
  <p><%= dish.description %></p>
  <%= link_to "Edit", edit_dish_path(dish), class: 'btn'%>
<% end %>
